[pytest]
markers =
    integration: live tests that call the real Anthropic API (deselected by default)
# Plain `pytest` excludes the live suite; opt in with `pytest -m integration`,
# which overrides this on the command line.
addopts = -m "not integration"
